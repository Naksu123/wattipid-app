// Style variables and tokens
import { COLORS, FONT_SIZE, FONT_WEIGHT, RADIUS, SPACING, SHADOWS } from './theme';

export const VARIABLES = {
  // Navigation
  tabBarHeight: 65,
  headerHeight: 60,
  
  // Layout
  pagePadding: SPACING.lg,
  
  // Transitions
  animationDuration: 300,
};

export { COLORS, FONT_SIZE, FONT_WEIGHT, RADIUS, SPACING, SHADOWS };
